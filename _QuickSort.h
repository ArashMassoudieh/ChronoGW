#pragma once
#include <vector>

using namespace std;
class CQuickSort
{
public:
	CQuickSort(void);
	~CQuickSort(void);
};

vector<double> QSort(vector<double> V);
vector<int> QSort(vector<int> V);